
const CONFIG = {
    // Falls back to the hardcoded string if the env variable isn't found
    API_URL: import.meta.env.VITE_API_URL || "https://referral-b7fd1-default-rtdb.firebaseio.com/subscribers.json"
};
